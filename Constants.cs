
namespace SnakeGame
{
    public static class Constants
    {
        public const int GameCols = 25;
        public const int GameRows = 25; // for a correct work the cols and rows must be the same
        public const int GameSpeed = 100; //in ms
        public const int TimeBetweenNumbersCountDown = 500; //in ms
        public const int GameOverDelay = 1000;
        public const int TimeBetweenDeadAnimation = 50;
        public const int CountDownFontSize= 100;
    }
}
